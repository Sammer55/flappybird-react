export const BASE_HEIGHT = 140;
export const WINDOW_HEIGHT = window.innerHeight;
export const WINDOW_WIDTH = window.innerWidth;

export const FAIR_PLAY_PIPES = 6;
export const FAIR_PLAY_PIPE_SIZE = {
  pipeUp: WINDOW_HEIGHT * 0.2 + "px",
  pipeDown: WINDOW_HEIGHT * 0.2 + "px",
};

export const PIPES_GAP = 80;
export const PIPES_SPACE_BETWEEN = 160;
export const PIPE_WIDTH = 52;
