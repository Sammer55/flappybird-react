export const BASE_HEIGHT = 140;
export const WINDOW_HEIGHT = window.innerHeight;
export const WINDOW_WIDTH = window.innerWidth;

export const FAIR_PLAY_PIPES = 6;
export const FAIR_PLAY_PIPE_SIZE = {
  pipeUp: WINDOW_HEIGHT * 0.1 + "px",
  pipeDown: WINDOW_HEIGHT * 0.1 + "px",
};

export const PIPES_GAP = 100;
export const PIPES_SPACE_BETWEEN = 180;
export const PIPE_WIDTH = 52;
export const PIPES_GENERATE = 100;
export const PIPE_MIN_HEIGHT = 20;
export const PIPE_MAX_HEIGHT = 60;

export const SCORE_DELAY = 3000;

export const GAME_DURATION = 150000;
